#!/bin/bash
# ==========================================
# 生产环境启动脚本（适用于1Panel等容器环境）
# ==========================================

set -e

echo "🚀 启动 Stock Manager..."

# 进入项目目录
cd "$(dirname "$0")/.."

# 初始化数据库
echo "💾 初始化数据库..."
node database/init.js

# 启动应用（前台运行）
echo "🌐 启动应用服务器..."
exec node server.js
