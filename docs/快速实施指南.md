# PWA + Capacitor 快速实施指南

## 🎯 目标

将股票系统改造为支持 PWA + Capacitor APK 的全平台应用，实现即时更新。

---

## 📋 准备工作检查清单

### 已完成的文件（本次创建）

- ✅ [public/js/platform-detector.js](../public/js/platform-detector.js) - 平台检测模块
- ✅ [public/js/update-manager.js](../public/js/update-manager.js) - 更新管理器
- ✅ [public/js/capacitor-bridge.js](../public/js/capacitor-bridge.js) - Capacitor 桥接
- ✅ [public/manifest.json](../public/manifest.json) - PWA 配置
- ✅ [public/service-worker.js](../public/service-worker.js) - Service Worker
- ✅ [public/css/platform.css](../public/css/platform.css) - 平台样式
- ✅ [server.js](../server.js) - 已添加版本检查 API

### 需要准备的资源

- [ ] 应用图标 192x192px (命名为 `icon-192.png`)
- [ ] 应用图标 512x512px (命名为 `icon-512.png`)
- [ ] 可选：启动画面截图

---

## 🚀 第一阶段：PWA 实施（预计 2 小时）

### 步骤 1：准备应用图标

```bash
# 创建图标目录
mkdir -p public/images

# 将准备好的图标复制到目录
# icon-192.png (192x192)
# icon-512.png (512x512)
```

**临时方案（如果没有图标）：**
可以使用在线工具生成：
- https://www.pwabuilder.com/imageGenerator
- https://realfavicongenerator.net/

### 步骤 2：修改 index.html

在 `public/index.html` 的 `<head>` 标签中添加：

```html
<!-- PWA 配置 -->
<link rel="manifest" href="/manifest.json">
<meta name="mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="股票系统">
<link rel="apple-touch-icon" href="/images/icon-192.png">
<meta name="theme-color" content="#2196F3">

<!-- 平台适配样式 -->
<link rel="stylesheet" href="/css/platform.css">

<!-- 平台检测和更新管理（必须在其他脚本之前） -->
<script src="/js/platform-detector.js"></script>
<script src="/js/update-manager.js"></script>
<script src="/js/capacitor-bridge.js"></script>
```

在 `<body>` 底部（在所有内容之后）添加：

```html
<!-- 安装提示横幅 -->
<div id="installBanner" class="install-banner" style="display: none;">
  <div class="banner-content">
    <div class="banner-icon">📱</div>
    <div class="banner-text">
      <strong>安装股票系统</strong>
      <span>快速访问，离线使用</span>
    </div>
    <button id="installPWABtn" class="banner-btn">安装 PWA</button>
    <button id="downloadAPKBtn" class="banner-btn">下载 APK</button>
    <button id="closeBannerBtn" class="banner-close">✕</button>
  </div>
</div>

<script>
// 安装提示逻辑
(function() {
  const banner = document.getElementById('installBanner');
  const installBtn = document.getElementById('installPWABtn');
  const downloadBtn = document.getElementById('downloadAPKBtn');
  const closeBtn = document.getElementById('closeBannerBtn');

  let deferredPrompt;

  // 仅在浏览器中显示
  if (Platform.info.isBrowser) {
    // 检查是否已经关闭过
    if (!localStorage.getItem('installBannerClosed')) {
      banner.style.display = 'block';
    }

    // 根据平台显示对应按钮
    if (Platform.info.canInstallPWA) {
      installBtn.style.display = 'inline-block';
    }
    if (Platform.info.canShowAppDownload) {
      downloadBtn.style.display = 'inline-block';
    }
  }

  // PWA 安装提示
  window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault();
    deferredPrompt = e;
    installBtn.style.display = 'inline-block';
  });

  // 点击安装 PWA
  installBtn.addEventListener('click', async () => {
    if (deferredPrompt) {
      deferredPrompt.prompt();
      const { outcome } = await deferredPrompt.userChoice;
      console.log('用户选择:', outcome);
      if (outcome === 'accepted') {
        Native.toast('安装成功！');
      }
      deferredPrompt = null;
    }
    banner.style.display = 'none';
  });

  // 点击下载 APK
  downloadBtn.addEventListener('click', () => {
    window.location.href = '/downloads/stock-manager.apk';
  });

  // 关闭横幅
  closeBtn.addEventListener('click', () => {
    banner.style.display = 'none';
    localStorage.setItem('installBannerClosed', 'true');
  });
})();
</script>
```

### 步骤 3：测试 PWA 功能

```bash
# 启动开发服务器
npm start

# 在浏览器中访问（必须使用 HTTPS 或 localhost）
# http://localhost:3000
```

**测试要点：**
1. 打开 Chrome DevTools → Application → Manifest，检查配置是否正确
2. 打开 Chrome DevTools → Application → Service Workers，检查是否已注册
3. 在 Chrome 地址栏右侧应该会显示"安装"图标
4. 点击安装，测试是否能添加到主屏幕

### 步骤 4：部署到生产环境

```bash
# 确保配置了 HTTPS（PWA 必须）
# 部署代码
npm run deploy

# 或者使用 git 部署
git add .
git commit -m "feat: 添加 PWA 支持"
git push
```

---

## 📦 第二阶段：Capacitor APK 实施（预计 3-4 小时）

### 步骤 1：安装 Capacitor

```bash
# 安装 Capacitor CLI 和核心库
npm install @capacitor/core @capacitor/cli

# 初始化 Capacitor
npx cap init
# 输入以下信息：
# App name: 股票系统
# App ID: com.yourcompany.stockmanager
# Web asset directory: public
```

这会创建 `capacitor.config.json` 文件。

### 步骤 2：配置 Capacitor

编辑 `capacitor.config.json`：

```json
{
  "appId": "com.yourcompany.stockmanager",
  "appName": "股票系统",
  "webDir": "public",
  "server": {
    "url": "https://your-domain.com",
    "cleartext": false,
    "allowNavigation": [
      "your-domain.com"
    ]
  },
  "android": {
    "buildOptions": {
      "keystorePath": "",
      "keystoreAlias": ""
    }
  }
}
```

**重要：** 将 `your-domain.com` 替换为你的实际域名。

### 步骤 3：添加 Android 平台

```bash
# 安装 Android 平台
npm install @capacitor/android

# 添加 Android 项目
npx cap add android
```

这会在项目根目录创建 `android/` 文件夹。

### 步骤 4：安装 Android Studio（如果还没有）

1. 下载 Android Studio: https://developer.android.com/studio
2. 安装并启动 Android Studio
3. 安装必要的 SDK 组件（Android Studio 会自动提示）

### 步骤 5：配置签名证书（用于发布版本）

```bash
# 生成密钥库（首次使用）
keytool -genkey -v -keystore release-key.jks \
  -keyalg RSA -keysize 2048 -validity 10000 \
  -alias stock-manager

# 记住你设置的密码和别名
```

编辑 `android/app/build.gradle`，在 `android` 块中添加：

```gradle
android {
    ...
    signingConfigs {
        release {
            storeFile file("../../release-key.jks")
            storePassword "your-keystore-password"
            keyAlias "stock-manager"
            keyPassword "your-key-password"
        }
    }
    buildTypes {
        release {
            signingConfig signingConfigs.release
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}
```

### 步骤 6：同步代码到 Android 项目

```bash
# 同步 Web 代码到 Android
npx cap sync
```

### 步骤 7：构建 APK

**方法一：使用 Android Studio（推荐）**

```bash
# 打开 Android Studio
npx cap open android
```

在 Android Studio 中：
1. 等待 Gradle 同步完成
2. 点击 `Build` → `Build Bundle(s) / APK(s)` → `Build APK(s)`
3. 构建完成后，点击通知中的 `locate` 查看 APK

**方法二：使用命令行**

```bash
cd android
./gradlew assembleRelease
```

APK 输出路径：`android/app/build/outputs/apk/release/app-release.apk`

### 步骤 8：测试 APK

```bash
# 方法 1：安装到连接的 Android 设备
adb install android/app/build/outputs/apk/release/app-release.apk

# 方法 2：将 APK 复制到手机手动安装
```

**测试要点：**
1. APK 能否正常安装
2. 打开后是否加载网页
3. 检查平台检测是否正确（应该显示"Android 应用"）
4. 测试更新机制：修改网页内容，重启 APP 是否自动更新

### 步骤 9：部署 APK

```bash
# 创建下载目录
mkdir -p public/downloads

# 复制 APK
cp android/app/build/outputs/apk/release/app-release.apk \
   public/downloads/stock-manager.apk

# 部署到服务器
npm run deploy
```

---

## 🧪 第三阶段：测试与验证（预计 1 小时）

### PWA 测试清单

- [ ] 浏览器访问正常
- [ ] Service Worker 注册成功
- [ ] manifest.json 配置正确
- [ ] 显示"安装"提示
- [ ] 能够"添加到主屏幕"
- [ ] 从主屏幕打开，全屏显示
- [ ] 离线时能访问缓存内容
- [ ] 更新提示正常工作
- [ ] 平台检测显示 "PWA 应用"

### APK 测试清单

- [ ] APK 安装成功
- [ ] 打开后加载在线网页
- [ ] 平台检测显示 "Android 应用"
- [ ] 网页更新后，重启 APP 自动获取新版本
- [ ] 状态栏颜色正确
- [ ] 返回键功能正常
- [ ] 通知功能正常（如果使用）

### 跨平台测试清单

- [ ] 浏览器、PWA、APK 都能正常访问
- [ ] 使用同一账号登录，数据同步
- [ ] 三种方式的功能一致
- [ ] UI 在不同平台上正常显示

---

## 📝 日常更新流程

### 更新网页内容

```bash
# 1. 修改代码（HTML/CSS/JS）
# 2. 测试
npm start

# 3. 更新版本号（可选）
# 编辑 package.json，增加 version 字段

# 4. 部署
npm run deploy
```

**结果：**
- ✅ 浏览器用户：刷新即可看到更新
- ✅ PWA 用户：后台自动下载，提示刷新
- ✅ APK 用户：重启 APP 自动加载新版本

### 发布新的 APK 版本

**仅在以下情况需要：**
- 需要新的原生功能
- 修改 Capacitor 配置
- 升级 Capacitor 版本

```bash
# 1. 修改 Capacitor 配置或添加插件
# 2. 同步到 Android
npx cap sync

# 3. 构建新 APK
cd android && ./gradlew assembleRelease

# 4. 部署 APK
cp android/app/build/outputs/apk/release/app-release.apk \
   public/downloads/stock-manager.apk
```

---

## 🔧 常见问题排查

### PWA 无法安装

**问题：** 浏览器地址栏没有显示"安装"图标

**解决：**
1. 确保使用 HTTPS 或 localhost
2. 检查 manifest.json 是否正确加载
3. 检查 Service Worker 是否注册成功
4. Chrome DevTools → Application → Manifest，查看错误信息

### Service Worker 不工作

**问题：** 离线时无法访问，或更新不生效

**解决：**
```bash
# 1. 清除浏览器缓存和 Service Worker
# Chrome DevTools → Application → Storage → Clear site data

# 2. 检查 Service Worker 代码是否有语法错误
# Chrome DevTools → Console

# 3. 手动卸载并重新注册
navigator.serviceWorker.getRegistrations()
  .then(regs => regs.forEach(reg => reg.unregister()));
```

### APK 无法加载网页

**问题：** APK 打开后显示空白或错误

**解决：**
1. 检查 `capacitor.config.json` 中的 `server.url` 是否正确
2. 确保服务器已配置 CORS
3. 检查网络连接
4. 查看 Android Logcat 日志：
   ```bash
   adb logcat | grep -i "capacitor\|webview"
   ```

### APK 无法安装

**问题：** 手机提示"无法安装"

**解决：**
1. 允许"未知来源"安装：设置 → 安全 → 允许未知来源
2. 检查签名是否正确
3. 卸载旧版本后重新安装

### 平台检测不准确

**问题：** 显示错误的平台类型

**解决：**
```javascript
// 在浏览器控制台执行
console.log(Platform.info);

// 检查 window.Capacitor 是否存在
console.log(window.Capacitor);

// 刷新平台信息
Platform.refresh();
```

---

## 📚 额外资源

### 生成应用图标

- [PWA Builder Image Generator](https://www.pwabuilder.com/imageGenerator)
- [RealFaviconGenerator](https://realfavicongenerator.net/)
- [App Icon Generator](https://appicon.co/)

### 测试工具

- [Lighthouse](https://developers.google.com/web/tools/lighthouse) - PWA 审核工具
- Chrome DevTools → Application - 检查 PWA 配置
- Android Studio Device Manager - 测试 APK

### 文档参考

- [PWA 官方文档](https://web.dev/progressive-web-apps/)
- [Capacitor 官方文档](https://capacitorjs.com/docs)
- [Service Worker API](https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API)

---

## 🎉 完成！

恭喜！你已经成功实施了 PWA + Capacitor 混合方案。

**现在你的系统支持：**
- 🌐 浏览器访问（所有平台）
- 📱 PWA 安装（Android/iOS/桌面）
- 📦 APK 安装（Android）
- ⚡ 即时更新（所有方式）
- 💾 离线使用
- 🔔 通知功能

**记住：**
- 日常更新只需部署网页，无需重新打包 APK
- 仅在需要新原生功能时才发布新 APK
- 定期检查更新机制是否正常工作

---

## 📞 需要帮助？

如果遇到问题，请查看：
1. [PWA+Capacitor混合方案.md](./PWA+Capacitor混合方案.md) - 完整技术方案
2. [PWA实施方案.md](./PWA实施方案.md) - PWA 详细说明
3. [Capacitor实施方案.md](./Capacitor实施方案.md) - Capacitor 详细说明
